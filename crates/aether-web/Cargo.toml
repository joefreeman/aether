[package]
name = "aether-web"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "wasm-bindgen binding shell for aether-client: the browser's seam onto the shared client core. Keeps aether-client pure; owns every #[wasm_bindgen] and boundary DTO."

[lib]
# cdylib for the wasm artifact; rlib so the boundary logic stays unit-testable on the host target.
crate-type = ["cdylib", "rlib"]

[dependencies]
aether-client = { workspace = true }
aether-protocol = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
# Pinned to the installed `wasm-bindgen` CLI (0.2.104); the two must match exactly.
wasm-bindgen = "=0.2.104"
serde-wasm-bindgen = "0.6"
# The browser's clock. wasm32-unknown-unknown has no `SystemTime`, so anything dated — the blame
# label, a picker's commit ages — gets "now" from JS through here.
js-sys = "0.3"
